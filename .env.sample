# Dev Agent Environment Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit .env file to Git!

# GitHub Configuration (SECRETS - never commit to Git)
GITHUB_TOKEN=ghp_your_github_token_here

# LLM Configuration (SECRETS - for automatic translations)
OPENAI_API_KEY=sk-your_openai_api_key_here
GOOGLE_API_KEY=your_google_api_key_here

# Application Configuration (ENVIRONMENT - can be overridden per environment)
NODE_ENV=development
LOG_LEVEL=info

# Note: Project rules (github.owner, github.repo, branch patterns) are now stored in .dev-agent.json
# This follows the "Protocol as Code" principle and allows versioning of project configuration
DEV_AGENT_DB_PATH=G:\Path